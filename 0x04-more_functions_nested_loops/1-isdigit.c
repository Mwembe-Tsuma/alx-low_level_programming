#include "main.h"
/**
 * _isdigit- Prints a lowercase alphabet
 *@c: Character to be checked
 * Return: 1 if true and 0 otherwise
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	return (0);
}
